#include<stdio.h>
int main07()
{
	//数组元素类型 (*数组指针变量名) [数列]
	int arr[2][3] = { {1,2,3},{4,5,6} };
	//定义指向数组的指针
	int(*p1)[3] = arr;				//将二维数组的数组名赋值给指针p1
	int(*p2)[3] = &arr[0][0];		//取第一个元素的地址赋值给p2
	int(*p3)[3] = arr[0];			//取第一行地址赋值给p3
	
	printf("%d \n", p1[0][0]);
	return 0;
}